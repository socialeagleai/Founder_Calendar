# Copy to frontend/.env for local dev. VITE_* values are baked into the bundle
# at build time, so set them before `npm run build`.
VITE_API_URL=http://127.0.0.1:8000
VITE_GOOGLE_CLIENT_ID=
# Set to "true" to show the Google sign-in button (needs an authorized origin).
VITE_GOOGLE_SIGNIN=
